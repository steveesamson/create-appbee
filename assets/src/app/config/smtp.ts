import { Record } from "appbee";

const smtp: Record = {
  // sender: "Sender<sender@host.com>",
  // templateFile: "mail.html",
  // host: "smtp.host.com",
  // port: 465,
  // secure: true,
  // auth: {
  //   user: "hello@host.com",
  //   pass: "1234@abc",
  // },
  // maxConnections: 5,
  // maxMessages: 10,
};

export default smtp;
