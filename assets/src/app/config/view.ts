import { ViewConfig } from 'appbee';

const view: ViewConfig = {
  engine: 'stud',
  staticDir: 'public',
  viewDir: 'views',
  templateDir: 'template',
  uploadDir: 'uploads',
  indexFile: 'index',
};

export default view;
